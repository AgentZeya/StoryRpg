-- ==========================================
-- STORY RPG — CUSTOM BLACK UI (Rayfield Removed)
-- ✓ Same farm/attack/skill/safezone logic as before
-- ✓ Multi-select mobs preserved (click to toggle)
-- ✓ Custom black GUI (from Script 2 layout), white text
-- ✓ Scaled to ~90% size
-- ✓ Custom key-check screen replaces Rayfield KeySystem
-- ==========================================
local unpack = unpack or table.unpack

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")
local LocalPlayer = Players.LocalPlayer

-- Remotes
local remotesFolder = ReplicatedStorage:WaitForChild("Remotes", 5)
local requestAttackRemote = remotesFolder and remotesFolder:WaitForChild("RequestAttack", 5)
local skillCastRemote = remotesFolder and remotesFolder:WaitForChild("SkillCast", 5)

if not remotesFolder then warn("[StoryRPG] Remotes folder not found!") end
if not requestAttackRemote then warn("[StoryRPG] RequestAttack remote not found!") end
if not skillCastRemote then warn("[StoryRPG] SkillCast remote not found!") end

-- State
local isFarming = false
local autoAttack = false
local autoSkills = false
local autoSafeZone = false
local isHealingInSafeZone = false
local walkspeedActive = false
local walkspeedValue = 30
local farmDistance = 5
local safeZonePercent = 20

local selectedMobNames = {}   -- multi-select, empty = farm any mob
local currentTargetMob = nil

-- ==========================================
-- 🔑 KEY SYSTEM CONFIG
-- ==========================================
local VALID_KEY = "eyaganda"
local WHITELISTED_USERIDS = {
	1786667991123,
}

local function isWhitelisted()
	for _, id in ipairs(WHITELISTED_USERIDS) do
		if LocalPlayer.UserId == id then
			return true
		end
	end
	return false
end

local bypassKey = isWhitelisted()

-- ==========================================
-- HELPER FUNCTIONS
-- ==========================================
local function getBaseName(name)
	local base = string.gsub(name, "%d+$", "")
	base = string.gsub(base, "_+$", "")
	return base
end

local function getHRP(model)
	if not model then return nil end
	return model:FindFirstChild("HumanoidRootPart") or model:FindFirstChild("Torso") or model:FindFirstChild("UpperTorso") or model.PrimaryPart
end

local function getSafeZoneBasePart()
	local hz = Workspace:FindFirstChild("HealthZones")
	if not hz then return nil end
	for _, desc in ipairs(hz:GetDescendants()) do
		if desc:IsA("BasePart") then
			if desc.Name == "default" or desc.Name == "Circle" or (desc.Parent and desc.Parent.Name == "Platform") then
				return desc
			end
		end
	end
	return hz:FindFirstChildWhichIsA("BasePart", true)
end

local function getMobFolders()
	local folders = {}
	local runtime = Workspace:FindFirstChild("_Runtime")
	if runtime then
		local boss = runtime:FindFirstChild("Boss")
		local monsters = runtime:FindFirstChild("Monsters")
		if boss then table.insert(folders, boss) end
		if monsters then table.insert(folders, monsters) end
	end
	return folders
end

local function isMobSelected(name)
	local baseName = getBaseName(name)
	if #selectedMobNames == 0 then
		return true
	end
	for _, selectedName in ipairs(selectedMobNames) do
		if baseName:lower() == selectedName:lower() then
			return true
		end
	end
	return false
end

local function toggleMobSelection(mobName)
	for i, name in ipairs(selectedMobNames) do
		if name:lower() == mobName:lower() then
			table.remove(selectedMobNames, i)
			return
		end
	end
	table.insert(selectedMobNames, mobName)
end

-- ==========================================
-- BACKGROUND LOOPS (unchanged logic)
-- ==========================================

-- Anti-AFK
LocalPlayer.Idled:Connect(function()
	VirtualUser:CaptureController()
	VirtualUser:ClickTime2Now()
end)

-- Walkspeed
RunService.RenderStepped:Connect(function()
	if walkspeedActive then
		local char = LocalPlayer.Character
		if char then
			local hum = char:FindFirstChildOfClass("Humanoid")
			if hum then hum.WalkSpeed = walkspeedValue end
		end
	end
end)

-- Noclip
RunService.Stepped:Connect(function()
	if isFarming or isHealingInSafeZone then
		local char = LocalPlayer.Character
		if char then
			for _, part in ipairs(char:GetDescendants()) do
				if part:IsA("BasePart") then
					part.CanCollide = false
				end
			end
		end
	end
end)

-- Auto Attack
task.spawn(function()
	while true do
		if autoAttack and requestAttackRemote then
			pcall(function()
				local args = {[1] = 1, n = 1}
				requestAttackRemote:FireServer(unpack(args, 1, args.n))
			end)
			task.wait(0.15)
		else
			task.wait(0.5)
		end
	end
end)

-- Auto Skills
task.spawn(function()
	while true do
		if autoSkills and skillCastRemote then
			local char = LocalPlayer.Character
			local hrp = getHRP(char)
			local dir = hrp and hrp.CFrame.LookVector or Vector3.new(0, 0, -1)

			pcall(function()
				local args1 = {[1] = 1, [2] = dir, n = 2}
				skillCastRemote:InvokeServer(unpack(args1, 1, args1.n))
			end)
			task.wait(0.8)

			if autoSkills then
				pcall(function()
					local args2 = {[1] = 2, [2] = dir, n = 2}
					skillCastRemote:InvokeServer(unpack(args2, 1, args2.n))
				end)
			end
			task.wait(1.2)
		else
			task.wait(0.5)
		end
	end
end)

-- Safe Zone monitor
task.spawn(function()
	while true do
		task.wait(0.2)
		if autoSafeZone then
			local char = LocalPlayer.Character
			local hum = char and char:FindFirstChildOfClass("Humanoid")
			if hum and hum.Health > 0 then
				local healthPct = (hum.Health / hum.MaxHealth) * 100
				if healthPct <= safeZonePercent and not isHealingInSafeZone then
					local targetPart = getSafeZoneBasePart()
					if targetPart then
						isHealingInSafeZone = true
					end
				elseif isHealingInSafeZone and hum.Health >= (hum.MaxHealth * 0.98) then
					isHealingInSafeZone = false
				end
			else
				isHealingInSafeZone = false
			end
		else
			isHealingInSafeZone = false
		end
	end
end)

RunService.RenderStepped:Connect(function()
	if autoSafeZone and isHealingInSafeZone then
		local char = LocalPlayer.Character
		local hrp = char and getHRP(char)
		local targetPart = getSafeZoneBasePart()
		if hrp and targetPart then
			hrp.AssemblyLinearVelocity = Vector3.zero
			hrp.AssemblyAngularVelocity = Vector3.zero
			hrp.CFrame = targetPart.CFrame * CFrame.new(0, 3, 0)
			pcall(function()
				firetouchinterest(hrp, targetPart, 0)
				firetouchinterest(hrp, targetPart, 1)
			end)
		end
	end
end)

-- Farm loop
RunService.RenderStepped:Connect(function()
	if isFarming and not isHealingInSafeZone then
		local char = LocalPlayer.Character
		local hrp = char and getHRP(char)
		local hum = char and char:FindFirstChildOfClass("Humanoid")

		if hrp and hum and hum.Health > 0 then
			if not currentTargetMob or not currentTargetMob.Parent then
				currentTargetMob = nil
			else
				local tHum = currentTargetMob:FindFirstChildOfClass("Humanoid")
				if not tHum or tHum.Health <= 0 then
					currentTargetMob = nil
				end
			end

			if not currentTargetMob then
				local closestDist = math.huge
				local searchPaths = getMobFolders()

				for _, folder in ipairs(searchPaths) do
					for _, mob in ipairs(folder:GetChildren()) do
						if mob:IsA("Model") and getHRP(mob) then
							if isMobSelected(mob.Name) then
								local tHum = mob:FindFirstChildOfClass("Humanoid")
								local tHrp = getHRP(mob)
								if tHum and tHrp and tHum.Health > 0 then
									local dist = (tHrp.Position - hrp.Position).Magnitude
									if dist < closestDist then
										closestDist = dist
										currentTargetMob = mob
									end
								end
							end
						end
					end
				end
			end

			if currentTargetMob then
				local tHrp = getHRP(currentTargetMob)
				if tHrp then
					local targetCFrame = tHrp.CFrame * CFrame.new(0, 0, farmDistance)
					if (hrp.Position - targetCFrame.Position).Magnitude > 0.5 then
						local lookAtCFrame = CFrame.lookAt(targetCFrame.Position, tHrp.Position)
						hrp.AssemblyLinearVelocity = Vector3.zero
						hrp.AssemblyAngularVelocity = Vector3.zero
						hrp.CFrame = hrp.CFrame:Lerp(lookAtCFrame, 0.25)
					end
				end
			end
		end
	end
end)

-- ==========================================
-- UI CONSTRUCTION (Black theme, white text, 90% scale)
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "StoryRpg"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local buildMainUI -- forward declaration

-- Shared style helpers
local COLOR_BG_DARK   = Color3.fromRGB(0, 0, 0)
local COLOR_PANEL     = Color3.fromRGB(10, 10, 10)
local COLOR_PANEL_ALT = Color3.fromRGB(18, 18, 18)
local COLOR_STROKE_OFF = Color3.fromRGB(55, 55, 55)
local COLOR_STROKE_ON  = Color3.fromRGB(255, 255, 255)
local COLOR_TEXT = Color3.fromRGB(255, 255, 255)
local COLOR_TEXT_DIM = Color3.fromRGB(190, 190, 190)

local function roundCorner(inst, radius)
	local c = Instance.new("UICorner")
	c.CornerRadius = UDim.new(0, radius or 5)
	c.Parent = inst
	return c
end

local function addStroke(inst, color, thickness)
	local s = Instance.new("UIStroke")
	s.Color = color or COLOR_STROKE_OFF
	s.Thickness = thickness or 1
	s.Parent = inst
	return s
end

-- ===== KEY SYSTEM SCREEN =====
if not bypassKey then
	local KeyFrame = Instance.new("Frame")
	KeyFrame.Size = UDim2.new(0, 270, 0, 150)
	KeyFrame.Position = UDim2.new(0.5, -135, 0.5, -75)
	KeyFrame.BackgroundColor3 = COLOR_BG_DARK
	KeyFrame.BorderSizePixel = 0
	KeyFrame.Parent = ScreenGui
	roundCorner(KeyFrame, 8)
	addStroke(KeyFrame, COLOR_STROKE_OFF, 1.5)

	local KeyTitle = Instance.new("TextLabel")
	KeyTitle.Size = UDim2.new(1, 0, 0, 26)
	KeyTitle.Position = UDim2.new(0, 0, 0, 8)
	KeyTitle.BackgroundTransparency = 1
	KeyTitle.Text = "eyaugh — Key Required"
	KeyTitle.TextColor3 = COLOR_TEXT
	KeyTitle.Font = Enum.Font.GothamBold
	KeyTitle.TextSize = 13
	KeyTitle.Parent = KeyFrame

	local KeySub = Instance.new("TextLabel")
	KeySub.Size = UDim2.new(1, -20, 0, 28)
	KeySub.Position = UDim2.new(0, 10, 0, 32)
	KeySub.BackgroundTransparency = 1
	KeySub.Text = "DM Eraña on Discord to get the key"
	KeySub.TextColor3 = COLOR_TEXT_DIM
	KeySub.Font = Enum.Font.Gotham
	KeySub.TextSize = 10
	KeySub.TextWrapped = true
	KeySub.Parent = KeyFrame

	local KeyBox = Instance.new("TextBox")
	KeyBox.Size = UDim2.new(1, -20, 0, 28)
	KeyBox.Position = UDim2.new(0, 10, 0, 66)
	KeyBox.BackgroundColor3 = COLOR_PANEL_ALT
	KeyBox.PlaceholderText = "Enter Key..."
	KeyBox.Text = ""
	KeyBox.TextColor3 = COLOR_TEXT
	KeyBox.PlaceholderColor3 = Color3.fromRGB(140, 140, 140)
	KeyBox.Font = Enum.Font.GothamMedium
	KeyBox.TextSize = 12
	KeyBox.ClearTextOnFocus = false
	KeyBox.Parent = KeyFrame
	roundCorner(KeyBox, 5)
	addStroke(KeyBox, COLOR_STROKE_OFF, 1)

	local SubmitBtn = Instance.new("TextButton")
	SubmitBtn.Size = UDim2.new(1, -20, 0, 28)
	SubmitBtn.Position = UDim2.new(0, 10, 0, 104)
	SubmitBtn.BackgroundColor3 = COLOR_PANEL_ALT
	SubmitBtn.Text = "SUBMIT"
	SubmitBtn.TextColor3 = COLOR_TEXT
	SubmitBtn.Font = Enum.Font.GothamBold
	SubmitBtn.TextSize = 12
	SubmitBtn.Parent = KeyFrame
	roundCorner(SubmitBtn, 5)
	addStroke(SubmitBtn, COLOR_STROKE_OFF, 1)

	local function tryKey()
		if KeyBox.Text == VALID_KEY then
			KeyFrame:Destroy()
			buildMainUI()
		else
			KeyTitle.Text = "Invalid Key!"
			KeyTitle.TextColor3 = Color3.fromRGB(255, 90, 90)
		end
	end

	SubmitBtn.MouseButton1Click:Connect(tryKey)
	KeyBox.FocusLost:Connect(function(enterPressed)
		if enterPressed then tryKey() end
	end)
else
	buildMainUI = buildMainUI -- placeholder, real call happens below after definition
end

-- ===== MAIN UI BUILDER =====
buildMainUI = function()
	-- Toggle Button
	local OpenToggleButton = Instance.new("TextButton")
	OpenToggleButton.Size = UDim2.new(0, 68, 0, 23)
	OpenToggleButton.Position = UDim2.new(0, 14, 0.45, 0)
	OpenToggleButton.BackgroundColor3 = COLOR_PANEL
	OpenToggleButton.Text = "TOGGLE UI"
	OpenToggleButton.TextColor3 = COLOR_TEXT
	OpenToggleButton.Font = Enum.Font.GothamBold
	OpenToggleButton.TextSize = 9
	OpenToggleButton.Active = true
	OpenToggleButton.Draggable = true
	OpenToggleButton.Parent = ScreenGui
	roundCorner(OpenToggleButton, 6)
	addStroke(OpenToggleButton, COLOR_STROKE_OFF, 1.5)

	-- Main Frame
	local MainFrame = Instance.new("Frame")
	MainFrame.Size = UDim2.new(0, 252, 0, 396)
	MainFrame.Position = UDim2.new(0.05, 0, 0.12, 0)
	MainFrame.BackgroundColor3 = COLOR_BG_DARK
	MainFrame.Active = true
	MainFrame.Draggable = true
	MainFrame.Parent = ScreenGui
	roundCorner(MainFrame, 8)
	addStroke(MainFrame, COLOR_STROKE_OFF, 1.5)

	-- Header
	local Header = Instance.new("Frame")
	Header.Size = UDim2.new(1, 0, 0, 29)
	Header.BackgroundTransparency = 1
	Header.Parent = MainFrame

	local Title = Instance.new("TextLabel")
	Title.Size = UDim2.new(1, -36, 1, 0)
	Title.Position = UDim2.new(0, 9, 0, 0)
	Title.BackgroundTransparency = 1
	Title.Text = "Story RPG By NIA"
	Title.TextColor3 = COLOR_TEXT
	Title.Font = Enum.Font.GothamBold
	Title.TextSize = 11
	Title.TextXAlignment = Enum.TextXAlignment.Left
	Title.Parent = Header

	local HideBtn = Instance.new("TextButton")
	HideBtn.Size = UDim2.new(0, 20, 0, 20)
	HideBtn.Position = UDim2.new(1, -24, 0, 4)
	HideBtn.BackgroundColor3 = COLOR_PANEL
	HideBtn.Text = "-"
	HideBtn.TextColor3 = COLOR_TEXT
	HideBtn.Font = Enum.Font.GothamBold
	HideBtn.TextSize = 11
	HideBtn.Parent = Header
	roundCorner(HideBtn, 5)
	addStroke(HideBtn, COLOR_STROKE_OFF, 1)

	-- Search Box
	local SearchBox = Instance.new("TextBox")
	SearchBox.Size = UDim2.new(1, -18, 0, 23)
	SearchBox.Position = UDim2.new(0, 9, 0, 32)
	SearchBox.BackgroundColor3 = COLOR_PANEL
	SearchBox.PlaceholderText = "Search Mobs..."
	SearchBox.Text = ""
	SearchBox.TextColor3 = COLOR_TEXT
	SearchBox.PlaceholderColor3 = Color3.fromRGB(130, 130, 130)
	SearchBox.Font = Enum.Font.GothamMedium
	SearchBox.TextSize = 9
	SearchBox.Parent = MainFrame
	roundCorner(SearchBox, 5)
	addStroke(SearchBox, COLOR_STROKE_OFF, 1)

	-- Mob List Scroller
	local Scroller = Instance.new("ScrollingFrame")
	Scroller.Size = UDim2.new(1, -18, 0, 99)
	Scroller.Position = UDim2.new(0, 9, 0, 61)
	Scroller.BackgroundColor3 = COLOR_PANEL
	Scroller.ScrollBarThickness = 3
	Scroller.CanvasSize = UDim2.new(0, 0, 0, 0)
	Scroller.AutomaticCanvasSize = Enum.AutomaticSize.Y
	Scroller.Parent = MainFrame
	roundCorner(Scroller, 5)
	addStroke(Scroller, COLOR_STROKE_OFF, 1)

	local Layout = Instance.new("UIListLayout")
	Layout.Padding = UDim.new(0, 4)
	Layout.SortOrder = Enum.SortOrder.LayoutOrder
	Layout.Parent = Scroller

	local Padding = Instance.new("UIPadding")
	Padding.PaddingTop = UDim.new(0, 4)
	Padding.PaddingBottom = UDim.new(0, 4)
	Padding.PaddingLeft = UDim.new(0, 4)
	Padding.PaddingRight = UDim.new(0, 4)
	Padding.Parent = Scroller

	-- Row 1: Start & Stop Farm
	local StartBtn = Instance.new("TextButton")
	StartBtn.Size = UDim2.new(0.5, -12, 0, 25)
	StartBtn.Position = UDim2.new(0, 9, 0, 167)
	StartBtn.BackgroundColor3 = COLOR_PANEL
	StartBtn.Text = "START FARM"
	StartBtn.TextColor3 = COLOR_TEXT
	StartBtn.Font = Enum.Font.GothamBold
	StartBtn.TextSize = 8
	StartBtn.Parent = MainFrame
	roundCorner(StartBtn, 5)
	addStroke(StartBtn, COLOR_STROKE_OFF, 1)

	local StopBtn = Instance.new("TextButton")
	StopBtn.Size = UDim2.new(0.5, -12, 0, 25)
	StopBtn.Position = UDim2.new(0.5, 3, 0, 167)
	StopBtn.BackgroundColor3 = COLOR_PANEL
	StopBtn.Text = "STOP FARM"
	StopBtn.TextColor3 = COLOR_TEXT
	StopBtn.Font = Enum.Font.GothamBold
	StopBtn.TextSize = 8
	StopBtn.Parent = MainFrame
	roundCorner(StopBtn, 5)
	addStroke(StopBtn, COLOR_STROKE_OFF, 1)

	-- Row 2: Auto Attack & Auto Skills
	local AtkBtn = Instance.new("TextButton")
	AtkBtn.Size = UDim2.new(0.5, -12, 0, 25)
	AtkBtn.Position = UDim2.new(0, 9, 0, 196)
	AtkBtn.BackgroundColor3 = COLOR_PANEL
	AtkBtn.Text = "AUTO ATTACK: OFF"
	AtkBtn.TextColor3 = COLOR_TEXT
	AtkBtn.Font = Enum.Font.GothamBold
	AtkBtn.TextSize = 8
	AtkBtn.Parent = MainFrame
	roundCorner(AtkBtn, 5)
	local AtkStroke = addStroke(AtkBtn, COLOR_STROKE_OFF, 1)

	local SkillBtn = Instance.new("TextButton")
	SkillBtn.Size = UDim2.new(0.5, -12, 0, 25)
	SkillBtn.Position = UDim2.new(0.5, 3, 0, 196)
	SkillBtn.BackgroundColor3 = COLOR_PANEL
	SkillBtn.Text = "AUTO SKILLS: OFF"
	SkillBtn.TextColor3 = COLOR_TEXT
	SkillBtn.Font = Enum.Font.GothamBold
	SkillBtn.TextSize = 8
	SkillBtn.Parent = MainFrame
	roundCorner(SkillBtn, 5)
	local SkillStroke = addStroke(SkillBtn, COLOR_STROKE_OFF, 1)

	-- Row 3: Auto Safe Zone & Safe Zone % Input
	local SafeBtn = Instance.new("TextButton")
	SafeBtn.Size = UDim2.new(0.5, -12, 0, 25)
	SafeBtn.Position = UDim2.new(0, 9, 0, 226)
	SafeBtn.BackgroundColor3 = COLOR_PANEL
	SafeBtn.Text = "SAFE ZONE: OFF"
	SafeBtn.TextColor3 = COLOR_TEXT
	SafeBtn.Font = Enum.Font.GothamBold
	SafeBtn.TextSize = 8
	SafeBtn.Parent = MainFrame
	roundCorner(SafeBtn, 5)
	local SafeStroke = addStroke(SafeBtn, COLOR_STROKE_OFF, 1)

	local SafeHpFrame = Instance.new("Frame")
	SafeHpFrame.Size = UDim2.new(0.5, -12, 0, 25)
	SafeHpFrame.Position = UDim2.new(0.5, 3, 0, 226)
	SafeHpFrame.BackgroundColor3 = COLOR_PANEL
	SafeHpFrame.Parent = MainFrame
	roundCorner(SafeHpFrame, 5)
	addStroke(SafeHpFrame, COLOR_STROKE_OFF, 1)

	local SafeHpLabel = Instance.new("TextLabel")
	SafeHpLabel.Size = UDim2.new(1, 0, 0, 8)
	SafeHpLabel.Position = UDim2.new(0, 5, 0, 2)
	SafeHpLabel.BackgroundTransparency = 1
	SafeHpLabel.Text = "SAFE ZONE HP %"
	SafeHpLabel.TextColor3 = COLOR_TEXT_DIM
	SafeHpLabel.Font = Enum.Font.GothamBold
	SafeHpLabel.TextSize = 6
	SafeHpLabel.TextXAlignment = Enum.TextXAlignment.Left
	SafeHpLabel.Parent = SafeHpFrame

	local SafeHpBox = Instance.new("TextBox")
	SafeHpBox.Size = UDim2.new(1, -10, 0, 13)
	SafeHpBox.Position = UDim2.new(0, 5, 0, 10)
	SafeHpBox.BackgroundTransparency = 1
	SafeHpBox.Text = "20"
	SafeHpBox.TextColor3 = COLOR_TEXT
	SafeHpBox.Font = Enum.Font.GothamMedium
	SafeHpBox.TextSize = 9
	SafeHpBox.TextXAlignment = Enum.TextXAlignment.Left
	SafeHpBox.ClearTextOnFocus = false
	SafeHpBox.Parent = SafeHpFrame

	-- Row 4: Farm Distance & Walkspeed Toggle
	local DistFrame = Instance.new("Frame")
	DistFrame.Size = UDim2.new(0.5, -12, 0, 25)
	DistFrame.Position = UDim2.new(0, 9, 0, 256)
	DistFrame.BackgroundColor3 = COLOR_PANEL
	DistFrame.Parent = MainFrame
	roundCorner(DistFrame, 5)
	addStroke(DistFrame, COLOR_STROKE_OFF, 1)

	local DistLabel = Instance.new("TextLabel")
	DistLabel.Size = UDim2.new(1, 0, 0, 8)
	DistLabel.Position = UDim2.new(0, 5, 0, 2)
	DistLabel.BackgroundTransparency = 1
	DistLabel.Text = "FARM DISTANCE (STUDS)"
	DistLabel.TextColor3 = COLOR_TEXT_DIM
	DistLabel.Font = Enum.Font.GothamBold
	DistLabel.TextSize = 6
	DistLabel.TextXAlignment = Enum.TextXAlignment.Left
	DistLabel.Parent = DistFrame

	local DistBox = Instance.new("TextBox")
	DistBox.Size = UDim2.new(1, -10, 0, 13)
	DistBox.Position = UDim2.new(0, 5, 0, 10)
	DistBox.BackgroundTransparency = 1
	DistBox.Text = "5"
	DistBox.TextColor3 = COLOR_TEXT
	DistBox.Font = Enum.Font.GothamMedium
	DistBox.TextSize = 9
	DistBox.TextXAlignment = Enum.TextXAlignment.Left
	DistBox.ClearTextOnFocus = false
	DistBox.Parent = DistFrame

	local SpeedBtn = Instance.new("TextButton")
	SpeedBtn.Size = UDim2.new(0.5, -12, 0, 25)
	SpeedBtn.Position = UDim2.new(0.5, 3, 0, 256)
	SpeedBtn.BackgroundColor3 = COLOR_PANEL
	SpeedBtn.Text = "WALKSPEED: OFF"
	SpeedBtn.TextColor3 = COLOR_TEXT
	SpeedBtn.Font = Enum.Font.GothamBold
	SpeedBtn.TextSize = 8
	SpeedBtn.Parent = MainFrame
	roundCorner(SpeedBtn, 5)
	local SpeedStroke = addStroke(SpeedBtn, COLOR_STROKE_OFF, 1)

	-- Row 5: Anti-AFK Text
	local AfkText = Instance.new("TextLabel")
	AfkText.Size = UDim2.new(1, -18, 0, 22)
	AfkText.Position = UDim2.new(0, 9, 0, 285)
	AfkText.BackgroundColor3 = COLOR_PANEL
	AfkText.Text = "ANTI-AFK: ACTIVE"
	AfkText.TextColor3 = COLOR_TEXT
	AfkText.Font = Enum.Font.GothamBold
	AfkText.TextSize = 8
	AfkText.Parent = MainFrame
	roundCorner(AfkText, 5)
	addStroke(AfkText, COLOR_STROKE_OFF, 1)

	-- Status Text
	local StatusText = Instance.new("TextLabel")
	StatusText.Size = UDim2.new(1, -18, 0, 43)
	StatusText.Position = UDim2.new(0, 9, 0, 311)
	StatusText.BackgroundColor3 = COLOR_PANEL
	StatusText.Text = "Status: Idle (Select Mob)"
	StatusText.TextColor3 = COLOR_TEXT
	StatusText.Font = Enum.Font.GothamBold
	StatusText.TextSize = 8
	StatusText.TextWrapped = true
	StatusText.Parent = MainFrame
	roundCorner(StatusText, 5)
	addStroke(StatusText, COLOR_STROKE_OFF, 1)

	-- Status Updates
	task.spawn(function()
		while task.wait(0.5) do
			if isHealingInSafeZone then
				StatusText.Text = "Status: Healing in Safe Zone..."
			elseif isFarming then
				local list = table.concat(selectedMobNames, ", ")
				StatusText.Text = "Farming: " .. (list ~= "" and list or "Any Mob")
			end
		end
	end)

	-- UI LISTENERS
	OpenToggleButton.MouseButton1Click:Connect(function()
		MainFrame.Visible = not MainFrame.Visible
	end)

	HideBtn.MouseButton1Click:Connect(function()
		MainFrame.Visible = false
	end)

	UserInputService.InputBegan:Connect(function(input, gameProcessed)
		if not gameProcessed and input.KeyCode == Enum.KeyCode.RightShift then
			MainFrame.Visible = not MainFrame.Visible
		end
	end)

	AtkBtn.MouseButton1Click:Connect(function()
		autoAttack = not autoAttack
		AtkBtn.Text = autoAttack and "AUTO ATTACK: ON" or "AUTO ATTACK: OFF"
		AtkStroke.Color = autoAttack and COLOR_STROKE_ON or COLOR_STROKE_OFF
		AtkBtn.BackgroundColor3 = autoAttack and COLOR_PANEL_ALT or COLOR_PANEL
	end)

	SkillBtn.MouseButton1Click:Connect(function()
		autoSkills = not autoSkills
		SkillBtn.Text = autoSkills and "AUTO SKILLS: ON" or "AUTO SKILLS: OFF"
		SkillStroke.Color = autoSkills and COLOR_STROKE_ON or COLOR_STROKE_OFF
		SkillBtn.BackgroundColor3 = autoSkills and COLOR_PANEL_ALT or COLOR_PANEL
	end)

	SafeBtn.MouseButton1Click:Connect(function()
		autoSafeZone = not autoSafeZone
		SafeBtn.Text = autoSafeZone and "SAFE ZONE: ON" or "SAFE ZONE: OFF"
		SafeStroke.Color = autoSafeZone and COLOR_STROKE_ON or COLOR_STROKE_OFF
		SafeBtn.BackgroundColor3 = autoSafeZone and COLOR_PANEL_ALT or COLOR_PANEL
		if not autoSafeZone then isHealingInSafeZone = false end
	end)

	SafeHpBox:GetPropertyChangedSignal("Text"):Connect(function()
		local val = tonumber(SafeHpBox.Text)
		if val then safeZonePercent = val end
	end)

	DistBox:GetPropertyChangedSignal("Text"):Connect(function()
		local val = tonumber(DistBox.Text)
		if val then farmDistance = val end
	end)

	local wsCycle = {30, 50, 70}
	local wsIndex = 1
	SpeedBtn.MouseButton1Click:Connect(function()
		if not walkspeedActive then
			walkspeedActive = true
			wsIndex = 1
			walkspeedValue = wsCycle[wsIndex]
		else
			wsIndex = wsIndex + 1
			if wsIndex > #wsCycle then
				walkspeedActive = false
			else
				walkspeedValue = wsCycle[wsIndex]
			end
		end

		if walkspeedActive then
			SpeedBtn.Text = "WALKSPEED: " .. walkspeedValue
			SpeedStroke.Color = COLOR_STROKE_ON
			SpeedBtn.BackgroundColor3 = COLOR_PANEL_ALT
		else
			SpeedBtn.Text = "WALKSPEED: OFF"
			SpeedStroke.Color = COLOR_STROKE_OFF
			SpeedBtn.BackgroundColor3 = COLOR_PANEL
			local char = LocalPlayer.Character
			if char and char:FindFirstChildOfClass("Humanoid") then
				char:FindFirstChildOfClass("Humanoid").WalkSpeed = 16
			end
		end
	end)

	StartBtn.MouseButton1Click:Connect(function()
		isFarming = true
		local list = table.concat(selectedMobNames, ", ")
		StatusText.Text = "Farming: " .. (list ~= "" and list or "Any Mob")
	end)

	StopBtn.MouseButton1Click:Connect(function()
		isFarming = false
		currentTargetMob = nil
		StatusText.Text = "Status: Farming Stopped"
	end)

	-- MOB LIST POPULATION (multi-select)
	local function clearList()
		for _, child in pairs(Scroller:GetChildren()) do
			if child:IsA("TextButton") then child:Destroy() end
		end
	end

	local function updateMobList()
		clearList()
		local query = string.lower(SearchBox.Text)
		local aliveMobs = {}

		local searchPaths = getMobFolders()
		for _, folder in ipairs(searchPaths) do
			for _, mob in ipairs(folder:GetChildren()) do
				if mob:IsA("Model") and getHRP(mob) then
					local hum = mob:FindFirstChildOfClass("Humanoid")
					if hum and hum.Health > 0 then
						local baseName = getBaseName(mob.Name)
						if query == "" or string.find(string.lower(baseName), query) then
							aliveMobs[baseName] = (aliveMobs[baseName] or 0) + 1
						end
					end
				end
			end
		end

		local sortedNames = {}
		for name in pairs(aliveMobs) do table.insert(sortedNames, name) end
		table.sort(sortedNames)

		for _, mobName in ipairs(sortedNames) do
			local count = aliveMobs[mobName]
			local isSelected = false
			for _, sel in ipairs(selectedMobNames) do
				if sel:lower() == mobName:lower() then isSelected = true break end
			end

			local btn = Instance.new("TextButton")
			btn.Size = UDim2.new(1, 0, 0, 22)
			btn.BackgroundColor3 = isSelected and COLOR_PANEL_ALT or COLOR_PANEL
			btn.Text = mobName .. " (x" .. count .. ")"
			btn.TextColor3 = COLOR_TEXT
			btn.Font = Enum.Font.GothamBold
			btn.TextSize = 8
			btn.Parent = Scroller

			roundCorner(btn, 4)
			addStroke(btn, isSelected and COLOR_STROKE_ON or COLOR_STROKE_OFF, 1)

			btn.MouseButton1Click:Connect(function()
				toggleMobSelection(mobName)
				currentTargetMob = nil
				updateMobList()
			end)
		end
	end

	SearchBox:GetPropertyChangedSignal("Text"):Connect(updateMobList)

	task.spawn(function()
		while task.wait(2.5) do
			updateMobList()
		end
	end)

	updateMobList()
end

-- Kick off UI: skip key screen if whitelisted
if bypassKey then
	buildMainUI()
end
